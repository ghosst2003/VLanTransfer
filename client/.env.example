# Signaling server WebSocket URL
# Set this to your Render server URL (e.g., wss://vlan-transfer-server.onrender.com)
# Leave empty to use window.location.host (same-host deployment)
VITE_SIGNALING_URL=
